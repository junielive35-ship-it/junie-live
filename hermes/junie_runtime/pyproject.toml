[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "junie-runtime"
version = "0.1.0"
description = "Shared runtime primitives for Junie Live Hermes profiles"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
